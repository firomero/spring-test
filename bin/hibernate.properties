hibernate.connection.url=jdbc:postgresql://10.128.24.155/almacenes_v1
hibernate.connection.driver_class=org.postgresql.Driver
hibernate.connection.username=juancarlos
hibernate.connection.password=juancarlos
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

hibernate.hbm2ddl.auto=update

# The initial number of connections that are created when the pool is started.
dbcp.initialSize=1
#The maximum number of active connections that can be allocated from this pool 
#at the same time, or zero for no limit.
dbcp.maxActive=100

#The maximum number of active connections that can remain idle in the pool, 
#without extra ones being released, or zero for no limit.
dbcp.maxIdle=10

#The minimum number of active connections that can remain idle in the pool, 
#without extra ones being created, or zero to create none.
dbcp.minIdle=10

#The maximum number of milliseconds that the pool will wait (when there are 
#no available connections) for a connection to be returned before throwing 
#an exception, or -1 to wait indefinitely.
dbcp.maxWait=-1

#The SQL query that will be used to validate connections from this pool before 
#returning them to the caller. If specified, this query MUST be an SQL SELECT 
#statement that returns at least one row.
dbcp.validationQuery=

hibernate.dbcp.whenExhaustedAction=1

hibernate.dbcp.testOnBorrow=true
hibernate.dbcp.testOnReturn=true

hibernate.dbcp.ps.maxActive=10
hibernate.dbcp.ps.maxIdle=10

hibernate.dbcp.ps.maxWait=180000
 
hibernate.dbcp.ps.whenExhaustedAction=1